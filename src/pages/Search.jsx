import { useSelector } from "react-redux";

const Search = () => {
    // const loginauthdata = useSelector((state) => state.loginauths);
    // console.log(loginauthdata,"login")
    return(
        <>
        Search
        </>
    )
}
export default Search;